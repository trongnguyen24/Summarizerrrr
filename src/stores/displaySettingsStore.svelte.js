// @ts-nocheck
import { writable } from 'svelte/store'

export const fontSizeIndex = writable(2) // Default to prose-lg
export const widthIndex = writable(1) // Default to max-w-3xl
export const fontFamilyIndex = writable(0) // Default to font-default
