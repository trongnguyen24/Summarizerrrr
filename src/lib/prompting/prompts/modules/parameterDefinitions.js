// @ts-nocheck
import { lengthDefinitions } from './lengthDefinitions.js'
import { formatDefinitions } from './formatDefinitions.js'
import { toneDefinitions } from './toneDefinitions.js'

export const parameterDefinitions = {
  length: lengthDefinitions,
  format: formatDefinitions,
  tone: toneDefinitions,
  // Thêm các định nghĩa tham số khác tại đây nếu cần
}
